#include <stdio.h>
#include<stdint.h>
//#include "shape.h"
#include "rectangle.h"

/*
// Encapsulation
int main(){

 Shape s1, s2;

 Shape_ctor  ;
 Shape_ctor(&s2, 4 ,8);
 printf("shape s1(x = %d, y = %d)\n", Shape_getX(&s1), Shape_getY(&s1));
 printf("shape s2(x = %d, y = %d)\n", Shape_getX(&s2), Shape_getY(&s2));
 
 Shape_moveBy(&s1,2,2);
 Shape_moveBy(&s2,3,3);
 
  printf("shape s1(x = %d, y = %d)\n", Shape_getX(&s1), Shape_getY(&s1));
 printf("shape s2(x = %d, y = %d)\n", Shape_getX(&s2), Shape_getY(&s2));



}

*//*
//Inheritance
int main(){

 Rectangle r1,r2;
 
 Rectangle_ctor(&r1,2,4,5,7);
 Rectangle_ctor(&r2,5,7,8,4);
 
 printf("Rectangle r1(x = %d, y = %d, width = %d, height = %d)", r1.super.x, r1.super.y, r1.width, r1.height);
 printf("\nRectangle r2(x = %d, y = %d, width = %d, height = %d)", r2.super.x, r2.super.y, r2.width, r2.height);
 
 Shape_moveBy((shape *)&r1,-2,3);
 Shape_moveBy(&r2.super,2,-1);
 
 printf("\nRectangle r1(x = %d, y = %d, width = %d, height = %d)", r1.super.x, r1.super.y, r1.width, r1.height);
 printf("\nRectangle r2(x = %d, y = %d, width = %d, height = %d)", r2.super.x, r2.super.y, r2.width, r2.height);	
}
*/

//polymorphism
int main(){
	
}


